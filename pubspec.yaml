name: focus_widget
description: A focusable and blurable widget.
version: 1.0.0
homepage: https://github.com/gzlock/flutter_focus_widget

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: